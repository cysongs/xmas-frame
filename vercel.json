{
  "builds": [
    { "src": "src/**/*.ts", "use": "@vercel/node" }
  ],
  "buildCommand": "npm run typechain && tsc && frog vercel-build",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api"
    }
  ]
}